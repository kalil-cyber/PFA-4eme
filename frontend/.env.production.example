# Variables Vercel (Production) — Project → Settings → Environment Variables
# Remplacer par l’URL Render après déploiement backend

VITE_API_URL=https://tariki-api.onrender.com
VITE_WS_URL=https://tariki-api.onrender.com
VITE_ADMIN_BASE=/tariki-ops

# Optionnel — carte HD Mapbox
# VITE_MAPBOX_TOKEN=pk.xxx
