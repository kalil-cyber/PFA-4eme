VITE_API_URL=http://localhost:4000
VITE_WS_URL=http://localhost:4000
# Optionnel — satellite HD Mapbox ; sans token : plan OSM + satellite Esri (gratuit)
VITE_MAPBOX_TOKEN=your_mapbox_token_here
# URL secrète du portail admin (ne pas lier depuis l’UI publique)
VITE_ADMIN_BASE=/tariki-ops
